# ===========================================
# Production Environment Variables (Netlify)
# ===========================================
# This file is committed as a reference only.
# Netlify injects these at build time via its UI / netlify.toml.
# Do NOT put real secrets here if the repo is public.

VITE_API_BASE_URL=https://teledrivebackend.pxxlspace.cv
